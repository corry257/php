<?php
    //Outras operações 
    //abs() = valor absoluto o mesmo que módulo 
    $r = abs(200);
    echo "$r";

    //base_convert(); = conversor de bases 


    //ceil(), floor(), round() = arredonda para cima ou para baixo

    //hypot()

    //intdiv() = exibe a porção inteira de uma divisão

    //min(), max() = minimo e maximo entre dois números 

    // pi() = valor de pi 

    //pow() = potencia sem ordem de precedência

    //sin(), cos(), tan() = seno, consseno e tangente

    //sqrt() = raiz quadrada 